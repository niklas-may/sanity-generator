import {specialNumberSchema} from '../schemas/types/special-number'
import {localeStringSchema} from '../schemas/types/locale-string'
import {testDocumentSchema} from '../schemas/documents/test'

export const schemaTypes = [specialNumberSchema, localeStringSchema, testDocumentSchema]
