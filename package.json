{
  "name": "sanity-generator",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npx nodemon src/cli.ts",
    "dev:sanity": "cd ./playground/sanity && sanity dev",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "groqfmt-nodejs": "^0.0.4",
    "prettier": "^3.0.2",
    "prettier-plugin-groq": "^0.2.5",
    "typescript": "^5.1.6",
    "vite": "^4.4.5"
  }
}
